# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

#vagrant folder
/.vagrant
/vagrant

# nginx config file
*.conf

.mcp.json

# audit artifacts (generated, local state)
.ai-factory/audit/registry.json
.ai-factory/audit/certs-fingerprints.json
.ai-factory/audit/audit-report.txt